{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    }
  ],
  "env": {
    "JWT_SECRET": "@jwt_secret",
    "SUPER_ADMIN_PHONE": "13510420462"
  }
}
