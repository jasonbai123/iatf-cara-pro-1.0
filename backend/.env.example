# JWT 密钥 - 请生成一个强随机字符串
# 在 PowerShell 中运行: -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})
JWT_SECRET=your_jwt_secret_key_here

# 超级管理员手机号
SUPER_ADMIN_PHONE=13510420462

# 服务器端口（本地开发使用，Vercel 部署时会自动设置）
PORT=3000
